#include "Image.h"
 
int main() {
   Image monImage;
   monImage.testRegression();

   // Image monImage(5, 5);
   // Pixel monPix(3, 3, 3);
   // monImage.dessinerRectangle(1, 1, 3, 3, monPix);
   // monImage.afficherConsole();
   return 0;
}